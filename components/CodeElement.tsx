import React from "react";
import { CopyIcon } from "@chakra-ui/icons";
import { Code, useClipboard } from "@chakra-ui/react";

const CodeElement = (props: unknown) => {
  return <Code>Hello world</Code>;
};

export default CodeElement;
